apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: llm-platform
spec:
  template:
    spec:
      containers:
        - name: ollama
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
