apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: llm-platform
  labels:
    app.kubernetes.io/name: ollama
    app.kubernetes.io/part-of: llm-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ollama
  ports:
    - port: 11434
      targetPort: http
      protocol: TCP
      name: http
